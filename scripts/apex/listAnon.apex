// ListDemoAgain.practiceList();
// ListDemoAgain.printAnimalList2();

List<account> listAcc = new list<account>();
for (Integer i = 0; i < 5; i++) {
    account ac = new account();
    ac.Name = 'test acc trigger ' + i;
    //add account to list
    listAcc.add(ac);
}
insert listAcc;